/** Harm index is a 0–100 classroom score of how badly disasters hurt the city. Higher is worse. */
export const HARM_INDEX_HELP =
  'Harm index is 0–100 (higher is worse). It rises with people killed, injuries, damaged zones, destroyed roads, and disaster cost. A quiet session stays near 0; a catastrophe approaches 100.';
