-- Seed first admin user (run AFTER schema.sql)
-- Default credentials: admin / classroom123
-- CHANGE PASSWORD IMMEDIATELY after first login!
--
-- Password hash generated with PBKDF2-SHA256, 100k iterations.
-- To generate a new hash, use: npm run seed-admin (see package.json)

-- Example placeholder — the seed script generates real hashes at deploy time.
-- Manual insert requires running the seed script locally.
